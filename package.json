{
  "name": "ai-e2e-tester",
  "private": true,
  "packageManager": "pnpm@10.30.1",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "dev:server": "turbo run dev --filter=@ai-e2e/server",
    "dev:web": "turbo run dev --filter=@ai-e2e/web",
    "db:push": "turbo run db:push --filter=@ai-e2e/server"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild"
    ]
  }
}
